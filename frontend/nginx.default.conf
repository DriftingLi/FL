# ===== 前端 Nginx 配置 =====
# 用于 Docker 容器中代理 API 请求到后端，托管静态资源
# 前端容器兼任 SSL 终止 + API 反代 + 静态文件托管
#
# 注意：${DOMAIN} 占位符由容器启动时 envsubst 自动替换为实际域名
# （来自 DOMAIN 环境变量，由 GitHub Secrets 注入）
# 域名策略：www 主站由官网门户（hrwai-portal, Nuxt）独立部署接管，
# 本配置只服务业务子域名（training./valuation./mentor./manage.）；
# 根域名请求 301 重定向到 www.根域名（门户）。

# ===== HTTP 80 端口：全部重定向到 HTTPS =====
server {
    listen 80;
    server_name _;

    # 健康检查端点：不重定向到 HTTPS
    location /health {
        add_header Content-Type text/plain;
        return 200 "ok";
    }

    return 301 https://$host$request_uri;
}

# ===== HTTPS 根域名 → www 主域名 301 重定向（门户） =====
server {
    listen 443 ssl;
    http2 on;
    server_name ${DOMAIN};
    ssl_certificate     /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    return 301 https://www.${DOMAIN}$request_uri;
}

# ===== HTTPS 业务子域名主服务器（不含 www，www 由门户接管） =====
server {
    listen 443 ssl;
    http2 on;
    server_name training.${DOMAIN} valuation.${DOMAIN} mentor.${DOMAIN} manage.${DOMAIN} recruit.${DOMAIN};

    # SSL 证书配置（证书文件挂载到 /etc/nginx/ssl/）
    ssl_certificate     /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache   shared:SSL:10m;
    ssl_session_timeout 10m;

    # 访问日志（生产环境建议关闭以减少 IO）
    access_log /var/log/nginx/access.log;
    error_log  /var/log/nginx/error.log warn;

    # 客户端上传大小限制（与后端 MAX_CONTENT_LENGTH_MB 对齐）
    client_max_body_size 250M;

    # 根目录
    root /usr/share/nginx/html;
    index index.html;

    # ===== API 反向代理 =====
    location /api/ {
        proxy_pass http://backend:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }

    # ===== 静态资源代理 =====
    location /static/ {
        proxy_pass http://backend:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_cache_bypass $http_upgrade;
    }

    # ===== 健康检查端点 =====
    location /health {
        access_log off;
        add_header Content-Type text/plain;
        return 200 "ok";
    }

    # ===== AI 助手（ADR-0001） =====
    # 旧 www/ai-assistant 请求由官网门户接管后不再到达本服务器；
    # training. 子域名下的 /ai-assistant 由 SPA 正常服务
    location /ai-assistant {
        try_files $uri /index.html;
    }

    # ===== SPA 路由回退 =====
    # Vue Router history 模式下，所有非文件路径回退到 index.html
    location / {
        try_files $uri $uri/ /index.html;

        # SPA 入口 HTML 禁止缓存：index.html 内容随发布变化，
        # 浏览器缓存旧 SPA 会导致部署后用户拿到旧应用（子块静态资源缓存不受影响）
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;

        # 静态资源缓存策略
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 30d;
            add_header Cache-Control "public, immutable";
        }
    }

    # ===== 安全头 =====
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # ===== 禁止访问隐藏文件 =====
    location ~ /\. {
        deny all;
        access_log off;
    }

    # ===== Gzip 压缩 =====
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml+rss
        image/svg+xml;
}
