/**
 * 环境配置 - 开发/生产环境分离
 *
 * 切换环境：将 ENV_MODE 改为 'development' 或 'production'
 * 说明：uni-app x 在 HBuilderX 中编译，不支持 process.env.NODE_ENV，
 *      需在打包发布前手动切换 ENV_MODE
 *
 * - 接口路径统一前缀 /api，响应格式 { code, message, data }
 * - H5 端使用完整 URL，依赖后端 CORS
 * - App / 小程序端使用完整地址
 */

/** 当前环境：'development' | 'production' */
export const ENV_MODE = 'development'

/** 是否开发环境 */
export const IS_DEV : boolean = ENV_MODE == 'development'

/** 开发环境 API 基础 URL（本地/局域网调试） */
const DEV_API_BASE_URL = 'http://192.168.0.140:8080/api'
/** 生产环境 API 基础 URL */
const PROD_API_BASE_URL = 'http://www.gccsmile.com:51820/api'

/**
 * API 基础 URL
 * - H5 端使用完整 URL，依赖后端 CORS
 * - App / 小程序端使用完整地址
 */
export const API_BASE_URL = IS_DEV ? DEV_API_BASE_URL : PROD_API_BASE_URL

/**
 * 请求超时时间（毫秒）
 * - 开发环境 8 秒：后端不可用时尽快回退到 mock 数据，避免长时间白屏
 * - 生产环境 15 秒：保证弱网下正常请求能完成
 */
export const REQUEST_TIMEOUT = IS_DEV ? 8000 : 15000

/** AI 长任务请求超时时间（毫秒） */
export const AI_REQUEST_TIMEOUT = 120000

/** 是否启用 mock 数据回退（网络失败时使用，仅开发环境） */
export const ENABLE_MOCK_FALLBACK = IS_DEV

/** 是否开启详细日志（仅开发环境） */
export const ENABLE_DEBUG_LOG = IS_DEV
