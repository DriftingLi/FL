<template>
    <!-- 学习进度区 —— T08 手术自 pages/courses/course-detail.uvue 逐段搬出（parent #646 / ADR-0007）。
         类名与结构逐字不变；原来直读 detail / learningProgress / learningCompleted，改为扁平数字 props。
         外层 v-if="isEnrolled" 由页面持有（门控是页面状态，不搬）。 -->
    <view class="progress-section">
        <view class="progress-header">
            <text class="progress-label">学习进度</text>
            <text class="progress-percent">{{ progress }}%</text>
        </view>
        <view class="progress-bar-wrap">
            <view class="progress-bar" :style="{ width: progress + '%' }"></view>
        </view>
        <text class="progress-detail">已完成 {{ completed }}/{{ totalChapters }} 章</text>
    </view>
</template>

<script setup lang="uts">
    const props = withDefaults(defineProps<{
        progress? : number
        completed? : number
        totalChapters? : number
    }>(), {
        progress: 0,
        completed: 0,
        totalChapters: 0
    })
</script>

<style>
    /* ===== 学习进度 ===== */
    .progress-section {
        background-color: #ffffff;
        padding: 24rpx 32rpx;
        border-top: 1rpx solid #f5f5f5;
    }

    .progress-header {
        flex-direction: row;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 12rpx;
    }

    .progress-label {
        font-size: 26rpx;
        color: #666666;
    }

    .progress-percent {
        font-size: 28rpx;
        font-weight: bold;
        color: #2979ff;
    }

    .progress-bar-wrap {
        height: 12rpx;
        background-color: #e8eaec;
        border-radius: 6rpx;
        overflow: hidden;
    }

    .progress-bar {
        height: 12rpx;
        background-color: #2979ff;
        border-radius: 6rpx;
    }

    .progress-detail {
        font-size: 22rpx;
        color: #999999;
        margin-top: 8rpx;
    }
</style>
