# ① 真机验证截图 · PR #1058（#905 移动端章节表格渲染）

设备：Redmi 23049RAD8C（`b32d8398`），Android 15，HBuilderX 调试基座 —— 按移动端
`docs/adr/0008-移动端验收门与证据.md` 的 ① 门（**①a：agent 出证**）留档，深链
`pages/courses/chapter-view?course_id=6&chapter_id=16`（「第一章 制动系统故障诊断」，正文含 4 列表格）。

## 图片

| 文件 | 构建 | 说明 |
| --- | --- | --- |
| `md-table-before.jpg` | 设备上既有旧构建（未含本票改动） | 正文表格渲染成**竖线原文**：每一行是**一个满宽文本节点**（`\| 故障现象 \| 可能原因 \| 排除方法 \| 紧急程度 \|`、`\|---------｜---------｜…`），本票要修的破口 |
| `md-table-after.jpg` | 本分支 head（`e921c040`，12:18 部署） | 同一章节同一位置渲染成**原生表格**：表头 4 个并列**等宽**单元格 + 表体分列 |

## 判据（不是靠肉眼看「像不像」，都有量出来的数）

1. **表头是 4 个等宽单元格**（`uiautomator dump` 的 bounds，修复后）：`故障现象 [75,1910][264,1968]`、
   `可能原因 [322,1910][511,1968]`、`排除方法 [569,1910][758,1968]`、`紧急程度 [816,1910][1005,1968]`
   —— 每格宽 **189px**（等分）、同一 y 带（1910–1968）；表体行 `制动失灵 [75,2048][264,2106]` 落在其下方。
2. **修复前同位置是整行一个节点**：`| 故障现象 | 可能原因 | 排除方法 | 紧急程度 |  [46,1761][1034,1836]`
   —— 满宽（46→1034）+ 竖线字符，即「原始文本」；分隔行 `|---------|---------|---------|---------|`
   同样整行可见（当屏可见的 5 行全部如此）。
3. **修复后整页 `content-desc` 里带竖线的节点数 = 0**（脚本按 `content-desc="[^"]*\|[^"]*"` 计数：
   修复前 5 条，修复后 0 条）。
4. **部署相对基线前进**（不用「前台是不是基座」这种恒真判据）：设备侧
   `…/apps/__UNI__1C1D180/www` 的 mtime 由基线 `11:49` 前进到 `12:18`；launch 日志打出
   `应用【叉车维修培训学员端跨端应用-md905】已启动` 与 `Token validated, isLoggedIn = [boolean] true`
   ⇒ 截图取自**本分支构建**，不是旧构建。
5. 取证窗口内 `cli launch` 的编译输出无 error，`uiautomator dump` 全程可读（页面未崩、未跳登录）。

## 手法（可复现）

```powershell
# 1) 等设备空闲（另一会话的 launch 派发 > 5 分钟前且取证目录静默 > 3 分钟）
# 2) 深链到目标页（真运行，不传 --compile）
& cli.exe launch app-android --pagePath pages/courses/chapter-view `
  --pageQuery "course_id=6&chapter_id=16" --project <本分支项目目录> --deviceId b32d8398
# 3) 有界轮询 uiautomator dump：出现表头单元格且竖线节点为 0 ⇒ PASS；再 adb exec-out screencap
# 4) 滑动需 1000ms（250ms 不被识别为滚动，本机实测）
```

> 本机实测：**点击与滑动注入可用**（`adb shell input tap` / `swipe 1000ms`）——与
> `scripts/device-capture.ps1` 记录的「注入被系统硬拒」不同，那条是**另一台设备**（`annibale`）的结论；
> 注入可注入性按仓库纪律**每次现测**，本次在 `b32d8398` 上现测通过。

## 与代码改动的对应

- `utils/markdown.uts`：`chapter` 档把 GFM 表格按**行**解析成 `table` 块（`items` = 该行单元格、
  `level` = 1 表头 / 0 正文），列数按表头行归一（少补空串、多则截断）；
- `pages/courses/chapter-view.uvue`：`table` 块渲染为「逐行 flex + 等宽单元格」，表头行加底色、加粗
  并补表格外框，行间距归零使同一张表连成一片；
- 交叉验证（非门、一次性脚本不入库）：migration 种子里的 **18/18 篇**章节正文喂进解析器镜像——
  解析出 37 张表、表数不符 0、chapter 档残留竖线段落 0、featured 档零 `table` 块、无异常。
