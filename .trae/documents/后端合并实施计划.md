# 后端合并实施计划（阶段 1：1.1 ~ 1.9）

## Summary

将 `d:\叉车维修项目\残值评估\backend\`（module `forklift-backend`，Go 1.26.4，sqlc+pgx、viper、zap、gofpdf）合并到 `d:\叉车维修项目\维修培训\backend-go\`（module `forklift-training`，Go 1.24.0，GORM、godotenv、slog）作为 `internal/valuation/` 子模块。合并后形成单一 Go module、单一进程、单一端口，路由前缀 `/api/valuation/*`，共用同一 PostgreSQL 实例。

严格按 `合并项目计划.md` 的"阶段 1：后端合并"1.1~1.9 顺序执行。不修改维修培训已有业务代码（仅在 `cmd/server/main.go`、`internal/config/config.go`、`.env`/`.env.example`、`go.mod`/`go.sum` 追加 valuation 相关内容），不动前端，不 git init，不写 README。

## Current State Analysis

### 源项目（残值评估/backend）结构
- `internal/config/`：loader.go（viper 读 yaml）、database.go（NewPostgresPool）、logger.go（NewLogger）
- `internal/handler/`：8 个文件（battery/common/config/evaluation/health/historical/report/integration_test）
- `internal/model/`：3 个文件（battery/errors/evaluation）
- `internal/repository/`：8 个文件（battery + 6 个 sqlc 生成 + db.go + models.go）
- `internal/service/`：15 个文件（8 个业务 + 7 个测试）
- `internal/migration/`：migration.go（`//go:embed all:sql`）+ sql/（8 个 SQL 文件）
- `internal/middleware/middleware.go`：**不搬入**（使用维修培训的）
- `pkg/pdf/`：5 个文件（template/battery_template/font/radar/template_test）
- `cmd/server/static.go`：**不搬入**（embed.FS 模式）
- `assets/fonts/simhei.ttf`：字体文件，需复制到目标
- `sqlc.yaml` + `queries/*.sql`：sqlc 配置与查询，供未来重新生成

### 目标项目（维修培训/backend-go）现状
- `cmd/server/main.go`：加载 cfg → GORM InitDB → migratedb.RunMigrations → EnsureDefaultUsers → api.NewRouter → 启动
- `internal/api/router.go`：NewRouter(cfg, db) 创建 Gin 引擎，注册 14 个业务路由组
- `internal/config/config.go`：Config 结构体，godotenv + getenv 模式
- `internal/middleware/middleware.go`：JWTAuth/RequestID/Logger/Recovery/CORS/RoleRequired
- `internal/migrate/migrate.go`：RunMigrations(dsn, direction)，用 iofs 从 `migrations/` 目录读取
- `migrations/000001_init.up/down.sql`：维修培训 19 张表
- `go.mod`：go 1.24.0，pgx v5.5.5 (indirect)，无 gofpdf/viper/zap

### 关键依赖差异
| 依赖 | 源项目版本 | 目标项目版本 | 合并后 |
|------|----------|------------|-------|
| Go | 1.26.4 | 1.24.0 | 1.26.4 |
| gin | v1.12.0 | v1.10.0 | v1.12.0 |
| golang-migrate | v4.19.1 | v4.18.1 | v4.19.1 |
| pgx/v5 | v5.10.0 (direct) | v5.5.5 (indirect) | v5.10.0 (direct) |
| gofpdf | v1.16.2 | 无 | v1.16.2 (新增) |
| zap | v1.28.0 | 无 | v1.28.0 (新增) |
| viper | v1.21.0 | 无 | **不引入**（去掉，改用 os.Getenv） |

### 需要解决的 5 个关键问题
1. **integration_test.go** 依赖 `forklift-backend/internal/config`（NewPostgresPool）和 `forklift-backend/internal/middleware`（RecoveryMiddleware/CORSMiddleware），这两个包不搬入 valuation 子包
2. **font.go** 硬编码 Windows 绝对路径 `d:\叉车残值评估系统\backend\assets\fonts\simhei.ttf` 作为兜底
3. **battery.go** GenerateReport 硬编码 `pdfDir := "./storage/reports"`，应改用配置注入
4. **migration.go** 用 `//go:embed all:sql`，但合并后迁移文件移到 `backend-go/migrations/`，需改用 filesystem 读取
5. **loader.go** 用 viper 读 yaml，需改为不读 yaml（统一走 .env）

## Proposed Changes

### 1.1 创建 valuation 子包目录

在 `d:\叉车维修项目\维修培训\backend-go\internal\valuation\` 下创建子目录：
- `config/`
- `handler/`
- `model/`
- `repository/`
- `service/`
- `migration/`（不再建 `sql/` 子目录，因为迁移 SQL 合并到 `backend-go/migrations/`）

同时创建：
- `d:\叉车维修项目\维修培训\backend-go\pkg\pdf\`（已存在 pkg/，新增 pdf/ 子目录）
- `d:\叉车维修项目\维修培训\backend-go\assets\fonts\`（存放 simhei.ttf）
- `d:\叉车维修项目\维修培训\backend-go\queries\`（存放 sqlc 查询文件，供未来重新生成）

**执行命令**（PowerShell）：
```powershell
cd d:\叉车维修项目\维修培训\backend-go
New-Item -ItemType Directory -Force -Path internal\valuation\config, internal\valuation\handler, internal\valuation\model, internal\valuation\repository, internal\valuation\service, internal\valuation\migration, pkg\pdf, assets\fonts, queries
```

### 1.2 搬移并改造残值评估后端代码

#### 1.2.1 文件搬运清单

**从 `残值评估\backend\internal\` 搬到 `维修培训\backend-go\internal\valuation\`：**

| 源文件 | 目标文件 |
|-------|---------|
| `internal/handler/battery.go` | `internal/valuation/handler/battery.go` |
| `internal/handler/common.go` | `internal/valuation/handler/common.go` |
| `internal/handler/config.go` | `internal/valuation/handler/config.go` |
| `internal/handler/evaluation.go` | `internal/valuation/handler/evaluation.go` |
| `internal/handler/health.go` | `internal/valuation/handler/health.go` |
| `internal/handler/historical.go` | `internal/valuation/handler/historical.go` |
| `internal/handler/report.go` | `internal/valuation/handler/report.go` |
| `internal/handler/integration_test.go` | `internal/valuation/handler/integration_test.go` |
| `internal/model/battery.go` | `internal/valuation/model/battery.go` |
| `internal/model/errors.go` | `internal/valuation/model/errors.go` |
| `internal/model/evaluation.go` | `internal/valuation/model/evaluation.go` |
| `internal/repository/battery.go` | `internal/valuation/repository/battery.go` |
| `internal/repository/brands.sql.go` | `internal/valuation/repository/brands.sql.go` |
| `internal/repository/coefficient_configs.sql.go` | `internal/valuation/repository/coefficient_configs.sql.go` |
| `internal/repository/db.go` | `internal/valuation/repository/db.go` |
| `internal/repository/evaluations.sql.go` | `internal/valuation/repository/evaluations.sql.go` |
| `internal/repository/historical_sales.sql.go` | `internal/valuation/repository/historical_sales.sql.go` |
| `internal/repository/models.go` | `internal/valuation/repository/models.go` |
| `internal/repository/part_configs.sql.go` | `internal/valuation/repository/part_configs.sql.go` |
| `internal/service/battery_rul.go` | `internal/valuation/service/battery_rul.go` |
| `internal/service/battery_rul_test.go` | `internal/valuation/service/battery_rul_test.go` |
| `internal/service/coefficient.go` | `internal/valuation/service/coefficient.go` |
| `internal/service/helpers_test.go` | `internal/valuation/service/helpers_test.go` |
| `internal/service/kbrand.go` | `internal/valuation/service/kbrand.go` |
| `internal/service/kcondition.go` | `internal/valuation/service/kcondition.go` |
| `internal/service/kcondition_test.go` | `internal/valuation/service/kcondition_test.go` |
| `internal/service/khours.go` | `internal/valuation/service/khours.go` |
| `internal/service/khours_test.go` | `internal/valuation/service/khours_test.go` |
| `internal/service/kmarket.go` | `internal/valuation/service/kmarket.go` |
| `internal/service/kothers_test.go` | `internal/valuation/service/kothers_test.go` |
| `internal/service/ktime.go` | `internal/valuation/service/ktime.go` |
| `internal/service/ktime_test.go` | `internal/valuation/service/ktime_test.go` |
| `internal/service/kwork.go` | `internal/valuation/service/kwork.go` |
| `internal/service/valuation.go` | `internal/valuation/service/valuation.go` |
| `internal/service/valuation_test.go` | `internal/valuation/service/valuation_test.go` |
| `internal/config/database.go` | `internal/valuation/config/database.go` |
| `internal/config/logger.go` | `internal/valuation/config/logger.go` |
| `internal/config/loader.go` | `internal/valuation/config/loader.go`（将改造） |

**从 `残值评估\backend\pkg\pdf\` 搬到 `维修培训\backend-go\pkg\pdf\`：**
- `template.go`、`battery_template.go`、`font.go`、`radar.go`、`template_test.go`

**从 `残值评估\backend\` 搬到 `维修培训\backend-go\`：**
- `sqlc.yaml` → `sqlc.yaml`（供未来重新生成 sqlc 代码）
- `queries/*.sql`（5 个文件）→ `queries/*.sql`
- `assets/fonts/simhei.ttf` → `assets/fonts/simhei.ttf`

**不搬入：**
- `internal/middleware/middleware.go`（使用维修培训的 middleware）
- `cmd/server/static.go`（embed.FS 模式，维修培训用本地 static/ 目录）
- `cmd/server/main.go`（参考其装配逻辑，但改造后写入目标 main.go）
- `internal/migration/migration.go` 和 `internal/migration/sql/`（迁移文件合并到 `backend-go/migrations/`，运行器复用维修培训的 `migrate.RunMigrations`）

**执行命令**（PowerShell，用 Move-Item 移动）：
```powershell
$src = "d:\叉车维修项目\残值评估\backend"
$dst = "d:\叉车维修项目\维修培训\backend-go"

# handler
Move-Item "$src\internal\handler\*.go" "$dst\internal\valuation\handler\"
# model
Move-Item "$src\internal\model\*.go" "$dst\internal\valuation\model\"
# repository
Move-Item "$src\internal\repository\*.go" "$dst\internal\valuation\repository\"
# service
Move-Item "$src\internal\service\*.go" "$dst\internal\valuation\service\"
# config
Move-Item "$src\internal\config\database.go" "$dst\internal\valuation\config\"
Move-Item "$src\internal\config\logger.go" "$dst\internal\valuation\config\"
Move-Item "$src\internal\config\loader.go" "$dst\internal\valuation\config\"
# pdf
Move-Item "$src\pkg\pdf\*.go" "$dst\pkg\pdf\"
# sqlc 配置与查询
Move-Item "$src\sqlc.yaml" "$dst\sqlc.yaml"
Move-Item "$src\queries\*.sql" "$dst\queries\"
# 字体文件
Move-Item "$src\assets\fonts\simhei.ttf" "$dst\assets\fonts\simhei.ttf"
```

#### 1.2.2 模块路径替换

搬运完成后，对所有搬入的 `.go` 文件执行模块路径替换：

| 原 import 路径 | 新 import 路径 |
|--------------|--------------|
| `forklift-backend/internal/handler` | `forklift-training/internal/valuation/handler` |
| `forklift-backend/internal/model` | `forklift-training/internal/valuation/model` |
| `forklift-backend/internal/repository` | `forklift-training/internal/valuation/repository` |
| `forklift-backend/internal/service` | `forklift-training/internal/valuation/service` |
| `forklift-backend/internal/config` | `forklift-training/internal/valuation/config` |
| `forklift-backend/internal/migration` | `forklift-training/internal/valuation/migration`（如有引用） |
| `forklift-backend/pkg/pdf` | `forklift-training/pkg/pdf` |

**执行方式**：用 PowerShell 脚本对 `internal/valuation/` 和 `pkg/pdf/` 下所有 `.go` 文件批量替换。

```powershell
$dst = "d:\叉车维修项目\维修培训\backend-go"
$files = @()
$files += Get-ChildItem -Path "$dst\internal\valuation" -Recurse -Filter *.go
$files += Get-ChildItem -Path "$dst\pkg\pdf" -Recurse -Filter *.go

foreach ($f in $files) {
    $content = Get-Content $f.FullName -Raw
    $content = $content -replace 'forklift-backend/internal/handler', 'forklift-training/internal/valuation/handler'
    $content = $content -replace 'forklift-backend/internal/model', 'forklift-training/internal/valuation/model'
    $content = $content -replace 'forklift-backend/internal/repository', 'forklift-training/internal/valuation/repository'
    $content = $content -replace 'forklift-backend/internal/service', 'forklift-training/internal/valuation/service'
    $content = $content -replace 'forklift-backend/internal/config', 'forklift-training/internal/valuation/config'
    $content = $content -replace 'forklift-backend/internal/migration', 'forklift-training/internal/valuation/migration'
    $content = $content -replace 'forklift-backend/pkg/pdf', 'forklift-training/pkg/pdf'
    Set-Content -Path $f.FullName -Value $content -NoNewline
}
```

#### 1.2.3 特殊文件改造

**A. `internal/valuation/config/loader.go` 改造**（去掉 viper 读 yaml，改用 os.Getenv）：

将整个文件重写为：
- 删除 viper import 和所有 viper 调用
- 保留 `Config`/`ServerConfig`/`DatabaseConfig`/`LogConfig`/`StorageConfig` 结构体（其他文件引用）
- 新增 `LoadFromEnv(databaseURL string, corsOrigins []string) *Config` 函数，从环境变量读取 valuation 专属配置
- 保留 `Load(configPath string)` 函数签名但改为从环境变量读取（兼容性，main.go 不再调用它）

**B. `pkg/pdf/font.go` 改造**（删除硬编码 Windows 绝对路径）：

修改 `findFontFile()` 函数：
- 删除 `d:\叉车残值评估系统\backend\assets\fonts\simhei.ttf` 和 `d:/叉车残值评估系统/backend/assets/fonts/simhei.ttf` 兜底路径
- 保留可执行文件目录、工作目录查找逻辑
- 新增：查找 `backend-go/assets/fonts/simhei.ttf`（项目根目录场景）

**C. `internal/valuation/handler/battery.go` 改造**（PDF 目录配置化）：

修改 `BatteryHandler` 结构体，新增 `pdfOutputDir string` 字段：
```go
type BatteryHandler struct {
    repo         *repository.BatteryRepository
    service      *service.BatteryRULService
    logger       *zap.Logger
    pdfOutputDir string  // 新增
}
```
修改 `NewBatteryHandler` 签名，新增 `pdfOutputDir string` 参数。
修改 `GenerateReport` 方法中 `pdfDir := "./storage/reports"` 改为 `pdfDir := h.pdfOutputDir`（若为空则回退到 `./storage/reports`）。

**D. `internal/valuation/handler/integration_test.go` 改造**（去除 config/middleware 依赖）：

修改 `setupTestEnv` 函数：
- 删除 `"forklift-training/internal/valuation/config"` import（注意：搬入后路径已是 valuation/config）
- 删除 `"forklift-training/internal/middleware"` import（这个包不会被搬入 valuation）
- 替换 `config.NewPostgresPool(ctx, dsn, 5, 1, 60)` → 直接用 `pgxpool.New(ctx, dsn)`（简化，不需要连接池参数化）
- 替换 `middleware.RecoveryMiddleware(logger)` → `gin.Recovery()`
- 替换 `middleware.CORSMiddleware([]string{"*"})` → 直接不挂 CORS（测试不需要）或用 `gin.Logger()`
- 保留测试逻辑不变

**E. `internal/valuation/handler/health.go` 微调**（可选）：
- `Service: "forklift-valuation-backend"` 保留不变（标识子模块）

### 1.3 合并 go.mod / go.sum

修改 `d:\叉车维修项目\维修培训\backend-go\go.mod`：

1. `go 1.24.0` → `go 1.26.4`
2. 在 require 块新增 direct 依赖：
   - `github.com/jackc/pgx/v5 v5.10.0`（从 indirect 升级为 direct）
   - `github.com/jung-kurt/gofpdf v1.16.2`
   - `go.uber.org/zap v1.28.0`
3. 升级现有依赖：
   - `github.com/gin-gonic/gin v1.10.0` → `v1.12.0`
   - `github.com/golang-migrate/migrate/v4 v4.18.1` → `v4.19.1`
4. **不新增 viper**（loader.go 改造后不需要）
5. 删除 `github.com/jackc/pgx/v5 v5.5.5 // indirect` 行（升级后变为 direct）

执行：
```powershell
cd d:\叉车维修项目\维修培训\backend-go
go mod tidy
```
`go mod tidy` 会自动补充间接依赖（如 `github.com/jackc/pgx/v5` 的子包、`go.uber.org/multierr` 等）并整理 go.sum。

### 1.4 合并数据库迁移

#### 1.4.1 新建迁移文件

在 `d:\叉车维修项目\维修培训\backend-go\migrations\` 下新建：

**`000002_valuation.up.sql`**（合并源 000001_init + 000002_brand_models + 000003_battery_rul）：
- 创建 8 张表：evaluations、evaluation_items、brands（**含 models JSONB 列**）、part_configs、coefficient_configs、historical_sales、battery_evaluations、battery_cycle_features
- brands 表的 CREATE TABLE 语句中直接包含 `models JSONB NOT NULL DEFAULT '[]'::jsonb` 列（避免后续 ALTER）
- 索引一并创建

**`000002_valuation.down.sql`**：
```sql
DROP TABLE IF EXISTS battery_cycle_features;
DROP TABLE IF EXISTS battery_evaluations;
DROP TABLE IF EXISTS historical_sales;
DROP TABLE IF EXISTS coefficient_configs;
DROP TABLE IF EXISTS part_configs;
DROP TABLE IF EXISTS brands;
DROP TABLE IF EXISTS evaluation_items;
DROP TABLE IF EXISTS evaluations;
```

**`000003_valuation_seed.up.sql`**（合并源 000004_seed + 000005_seed_repair）：
- INSERT brands（13 个品牌，ON CONFLICT DO NOTHING）
- INSERT coefficient_configs（8 个系数，ON CONFLICT DO NOTHING）
- INSERT part_configs（电动 68 条 + 内燃 75 条，ON CONFLICT DO NOTHING）
- INSERT battery 相关注跳过（无 seed）
- 重复的 INSERT 因 ON CONFLICT DO NOTHING 安全

**`000003_valuation_seed.down.sql`**：
```sql
DELETE FROM part_configs;
DELETE FROM coefficient_configs;
DELETE FROM brands;
```

**`000004_brand_models_repair.up.sql`**（从源 000006 搬入）：
- 13 个 UPDATE brands SET models = ... 语句

**`000004_brand_models_repair.down.sql`**（从源 000006 搬入）：
```sql
UPDATE brands SET models = '[]'::jsonb WHERE models != '[]'::jsonb;
```

#### 1.4.2 迁移运行器处理

**决策：不搬入 `internal/valuation/migration/`，删除该目录。**

理由：
- 维修培训的 `internal/migrate/migrate.go` 已实现 `RunMigrations(dsn, direction)`，用 iofs 从 `migrations/` 目录读取所有迁移文件
- 合并后的迁移文件都在 `backend-go/migrations/` 下（000001~000004）
- `cmd/server/main.go` 已调用 `migratedb.RunMigrations(cfg.DatabaseURL, "up")`，会自动执行所有迁移
- 不需要重复的迁移运行器

**执行**：
```powershell
Remove-Item -Recurse -Force "d:\叉车维修项目\维修培训\backend-go\internal\valuation\migration"
```

源 `internal/migration/sql/` 目录的 SQL 内容已合并到 `backend-go/migrations/`，不需要单独保留。

### 1.5 扩展配置（.env + config.go）

#### 1.5.1 修改 `.env.example` 和 `.env`

在 `d:\叉车维修项目\维修培训\backend-go\.env.example` 和 `.env` 末尾追加：

```env
# ===== 残值评估模块 =====
VALUATION_PDF_OUTPUT_DIR=storage/reports
VALUATION_LOG_LEVEL=info
VALUATION_LOG_FORMAT=console
VALUATION_LOG_OUTPUT=stdout
VALUATION_DB_MAX_OPEN_CONNS=20
VALUATION_DB_MAX_IDLE_CONNS=5
VALUATION_DB_CONN_MAX_LIFETIME=3600
```

#### 1.5.2 修改 `internal/config/config.go`

在 `Config` 结构体新增 `Valuation ValuationConfig` 字段：
```go
type Config struct {
    // ... 现有字段 ...
    Valuation ValuationConfig
}

// ValuationConfig 残值评估模块配置
type ValuationConfig struct {
    PDFOutputDir       string
    LogLevel           string
    LogFormat          string
    LogOutput          string
    DBMaxOpenConns     int
    DBMaxIdleConns     int
    DBConnMaxLifetime  int
}
```

在 `Load()` 函数中初始化：
```go
cfg := &Config{
    // ... 现有初始化 ...
    Valuation: ValuationConfig{
        PDFOutputDir:      getenv("VALUATION_PDF_OUTPUT_DIR", "storage/reports"),
        LogLevel:          getenv("VALUATION_LOG_LEVEL", "info"),
        LogFormat:         getenv("VALUATION_LOG_FORMAT", "console"),
        LogOutput:         getenv("VALUATION_LOG_OUTPUT", "stdout"),
        DBMaxOpenConns:    20,
        DBMaxIdleConns:    5,
        DBConnMaxLifetime: 3600,
    },
}
```

### 1.6 修改主入口 `cmd/server/main.go`

在现有 `main()` 函数中，按顺序追加 valuation 装配逻辑：

```go
import (
    // 新增 import
    "context"
    "time"
    
    "github.com/jackc/pgx/v5/pgxpool"
    "go.uber.org/zap"
    
    vconfig "forklift-training/internal/valuation/config"
    vhandler "forklift-training/internal/valuation/handler"
    vrepo "forklift-training/internal/valuation/repository"
    vservice "forklift-training/internal/valuation/service"
    "forklift-training/pkg/pdf"
)

func main() {
    // ... 现有：加载 cfg、GORM InitDB、migratedb.RunMigrations、EnsureDefaultUsers、ensureUploadDirs ...
    // 注意：migratedb.RunMigrations 已执行所有迁移（含 valuation 的 000002~000004）

    // ===== 新增：valuation 模块装配 =====
    
    // 1. 初始化 zap 日志器
    vLogger, err := vconfig.NewLogger(vconfig.LogConfig{
        Level:  cfg.Valuation.LogLevel,
        Format: cfg.Valuation.LogFormat,
        Output: cfg.Valuation.LogOutput,
    })
    if err != nil {
        slog.Warn("valuation 日志初始化失败，降级到无操作日志", "error", err)
        vLogger = zap.NewNop()
    }
    defer vLogger.Sync()

    // 2. 创建 pgx 连接池（与 GORM 共用 DATABASE_URL）
    ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
    defer cancel()
    pool, err := vconfig.NewPostgresPool(ctx, cfg.DatabaseURL,
        cfg.Valuation.DBMaxOpenConns, cfg.Valuation.DBMaxIdleConns, cfg.Valuation.DBConnMaxLifetime)
    if err != nil {
        slog.Error("valuation pgx 连接池创建失败", "error", err)
        os.Exit(1)
    }
    defer pool.Close()

    // 3. 装配数据访问层
    queries := vrepo.New(pool)

    // 4. 装配业务服务（含内存加载器）
    coefficientLoader := vservice.NewCoefficientLoader(queries)
    brandLoader := vservice.NewBrandLoader(queries)
    partConfigLoader := vservice.NewPartConfigLoader(queries)
    if err := loadValuationConfig(ctx, coefficientLoader, brandLoader, partConfigLoader, vLogger); err != nil {
        vLogger.Fatal("加载 valuation 配置数据失败", zap.Error(err))
    }
    valuationSvc := vservice.NewValuationService(coefficientLoader, brandLoader, partConfigLoader)
    batterySvc := vservice.NewBatteryRULService()

    // 5. 装配 PDF 生成器
    pdfDir := cfg.Valuation.PDFOutputDir
    if err := os.MkdirAll(pdfDir, 0o755); err != nil {
        vLogger.Warn("创建 PDF 输出目录失败", zap.Error(err), zap.String("dir", pdfDir))
    }
    pdfGen := pdf.NewGenerator(pdfDir)

    // 6. 创建路由
    router := api.NewRouter(cfg, gormDB)

    // 7. 注册 valuation 路由
    vhandler.RegisterRoutes(router, cfg, vLogger, pool, queries, valuationSvc, brandLoader, batterySvc, pdfGen, pdfDir)

    // ... 现有：启动 HTTP 服务、优雅退出 ...
}

// loadValuationConfig 从数据库加载所有配置到内存加载器
func loadValuationConfig(
    ctx context.Context,
    coef *vservice.CoefficientLoader,
    brand *vservice.BrandLoader,
    parts *vservice.PartConfigLoader,
    logger *zap.Logger,
) error {
    if err := coef.LoadAll(ctx); err != nil {
        return err
    }
    if err := brand.LoadAll(ctx); err != nil {
        return err
    }
    if err := parts.LoadAll(ctx); err != nil {
        return err
    }
    return nil
}
```

### 1.7 创建 `internal/valuation/router.go`

新建 `d:\叉车维修项目\维修培训\backend-go\internal\valuation\router.go`：

```go
package handler

import (
    "github.com/gin-gonic/gin"
    "github.com/jackc/pgx/v5/pgxpool"
    "go.uber.org/zap"

    "forklift-training/internal/config"
    "forklift-training/internal/middleware"
    vrepo "forklift-training/internal/valuation/repository"
    vservice "forklift-training/internal/valuation/service"
    "forklift-training/pkg/pdf"
)

// RegisterRoutes 注册残值评估模块路由
// 所有路由挂在 /api/valuation/* 下，启用 JWTAuth 中间件
func RegisterRoutes(
    r *gin.Engine,
    cfg *config.Config,
    logger *zap.Logger,
    pool *pgxpool.Pool,
    queries *vrepo.Queries,
    valuationSvc *vservice.ValuationService,
    brandLoader *vservice.BrandLoader,
    batterySvc *vservice.BatteryRULService,
    pdfGen *pdf.Generator,
    pdfOutputDir string,
) {
    // 路由组：/api/valuation，启用 JWT 认证
    g := r.Group("/api/valuation")
    g.Use(middleware.JWTAuth(cfg))

    // 评估接口
    evalHandler := NewEvaluationHandler(queries, valuationSvc, logger)
    g.POST("/evaluations", evalHandler.Create)
    g.GET("/evaluations", evalHandler.List)
    g.GET("/evaluations/:id", evalHandler.Get)

    // PDF 报告接口
    reportHandler := NewReportHandler(queries, pdfGen, logger)
    g.POST("/evaluations/:id/report", reportHandler.Generate)
    g.GET("/evaluations/:id/report", reportHandler.Download)

    // 配置类接口
    configHandler := NewConfigHandler(queries, brandLoader, logger)
    g.GET("/part-configs", configHandler.ListPartConfigs)
    g.GET("/brands", configHandler.ListBrands)
    g.GET("/coefficients", configHandler.ListCoefficients)
    g.PUT("/coefficients/:key", configHandler.UpdateCoefficient)

    // 历史成交数据导入
    histHandler := NewHistoricalHandler(queries, logger)
    g.POST("/historical-sales/import", histHandler.Import)

    // 电池 RUL 评估
    batteryRepo := vrepo.NewBatteryRepository(pool)
    batteryHandler := NewBatteryHandler(batteryRepo, batterySvc, logger, pdfOutputDir)
    g.POST("/battery/evaluations", batteryHandler.Create)
    g.GET("/battery/evaluations", batteryHandler.List)
    g.GET("/battery/evaluations/:id", batteryHandler.Get)
    g.POST("/battery/evaluations/:id/report", batteryHandler.GenerateReport)
    g.GET("/battery/evaluations/:id/report", batteryHandler.DownloadReport)

    // 健康检查（valuation 子模块独立健康端点）
    healthHandler := NewHealthHandler()
    g.GET("/health", healthHandler.Check)
}
```

最终路由表（全部挂在 `/api/valuation/*`，需 JWT）：
- `POST /api/valuation/evaluations`
- `GET /api/valuation/evaluations`
- `GET /api/valuation/evaluations/:id`
- `POST /api/valuation/evaluations/:id/report`
- `GET /api/valuation/evaluations/:id/report`
- `GET /api/valuation/part-configs`
- `GET /api/valuation/brands`
- `GET /api/valuation/coefficients`
- `PUT /api/valuation/coefficients/:key`
- `POST /api/valuation/historical-sales/import`
- `POST /api/valuation/battery/evaluations`
- `GET /api/valuation/battery/evaluations`
- `GET /api/valuation/battery/evaluations/:id`
- `POST /api/valuation/battery/evaluations/:id/report`
- `GET /api/valuation/battery/evaluations/:id/report`
- `GET /api/valuation/health`

### 1.8 删除 embed 静态托管 + 修复测试

#### 1.8.1 不搬入 static.go（已在 1.2.1 说明）
- 不搬入 `残值评估/backend/cmd/server/static.go`
- 维修培训后端继续以本地 `static/` 目录方式提供静态资源
- 不嵌入前端 dist

#### 1.8.2 修复 integration_test.go（已在 1.2.3 D 说明）
- 移除 `forklift-training/internal/valuation/config` import（改用 pgxpool.New）
- 移除 `forklift-training/internal/middleware` import（改用 gin.Recovery()）
- 保留测试逻辑不变

#### 1.8.3 确认其他测试文件无外部依赖
- `service/*_test.go`：仅依赖 `forklift-training/internal/valuation/model`（路径替换后 OK）
- `pkg/pdf/template_test.go`：仅依赖 `forklift-training/internal/valuation/model`（路径替换后 OK）
- `handler/integration_test.go`：依赖修复后 OK

### 1.9 验证后端

```powershell
cd d:\叉车维修项目\维修培训\backend-go
go build ./...
go test ./...
```

**验证要点**：
1. `go build ./...` 无编译错误
2. `go test ./...` 全部通过（integration_test.go 在无 DB 环境会自动 t.Skip）
3. 不影响维修培训原有测试（auth/helpers/question/wrong_question 等）
4. valuation 单元测试（valuation_test/battery_rul_test/kcondition_test/khours_test/ktime_test/kothers_test）全绿
5. PDF 生成测试（template_test）通过（依赖 simhei.ttf 存在）

## Assumptions & Decisions

### 关键决策

1. **不引入 viper 依赖**：原计划要求新增 viper，但 loader.go 改造后用 os.Getenv 即可（与维修培训 config.go 风格一致），减少依赖。loader.go 保留但重写为不读 yaml。

2. **删除 `internal/valuation/migration/` 目录**：原计划要求保留并改造 migration.go，但维修培训的 `migrate.RunMigrations` 已能执行 `migrations/` 目录下所有迁移文件，不需要重复运行器。删除更简洁。

3. **迁移文件 brands 表直接包含 models 列**：源项目 000002_brand_models 是 ALTER TABLE 添加 models 列，合并到 000002_valuation.up.sql 时直接在 CREATE TABLE 中定义 models 列，避免多余 ALTER。

4. **000003_valuation_seed 合并 000004_seed + 000005_seed_repair**：两者都是 INSERT 语句（ON CONFLICT DO NOTHING），合并安全。000005 中的 brand models UPDATE 不包含在内（那是 000006 的内容）。

5. **000004_brand_models_repair 保留**：从源 000006 搬入，作为独立迁移补填型号数据。

6. **valuation 路由启用 JWTAuth**：符合"作为维修培训模块"的定位，学员登录后才能用残值评估功能。如需开放访问可去掉 `g.Use(middleware.JWTAuth(cfg))`。

7. **battery.go PDF 目录配置化**：原硬编码 `./storage/reports` 改为通过 `NewBatteryHandler` 参数注入 `pdfOutputDir`。

8. **font.go 删除硬编码绝对路径**：删除 `d:\叉车残值评估系统\backend\assets\fonts\simhei.ttf` 兜底，改为查找 `backend-go/assets/fonts/simhei.ttf`（项目根目录场景）。

9. **integration_test.go 修复方式**：用 `pgxpool.New(ctx, dsn)` 替代 `config.NewPostgresPool`，用 `gin.Recovery()` 替代 `middleware.RecoveryMiddleware`，删除 CORS（测试不需要）。

10. **zap 日志器保留**：valuation 模块继续用 zap（与代码深度耦合），不强制迁移到 slog。维修培训原有代码继续用 slog。两套日志并存。

### 假设

1. 维修培训的 `migrate.RunMigrations` 能正确识别 `000002_valuation` 等新迁移文件（golang-migrate 按文件名数字排序，OK）
2. pgxpool 与 GORM 共用同一 `DATABASE_URL`（`postgres://...` 格式，pgx 能解析）不会冲突
3. `simhei.ttf` 字体文件（9.7MB）能通过 `Move-Item` 正常移动
4. Go 1.26.4 升级后维修培训代码兼容（无破坏性变更）
5. `gin v1.12.0` 与 `gin-contrib/cors v1.7.2` 兼容

### 不在本次范围内
- 不修改维修培训已有业务代码（仅追加 valuation 相关内容到 main.go/config.go/.env/go.mod）
- 不动前端
- 不 git init
- 不写 README
- 不清理文件夹嵌套（阶段 3 才做）
- 不优化 PDF 模板
- 不迁移历史成交数据

## Verification Steps

按 1.9 顺序执行：

1. **编译验证**：
   ```powershell
   cd d:\叉车维修项目\维修培训\backend-go
   go build ./...
   ```
   预期：无编译错误。若有 import 路径遗漏，根据报错补替换。

2. **测试验证**：
   ```powershell
   go test ./...
   ```
   预期：
   - 维修培训原有测试全绿
   - valuation 单元测试全绿（service 层不依赖 DB）
   - integration_test.go 在无 DB 环境自动 skip
   - template_test.go 依赖 simhei.ttf，应通过

3. **vet 检查**：
   ```powershell
   go vet ./...
   ```
   预期：无警告。

4. **模块整理**：
   ```powershell
   go mod tidy
   ```
   预期：go.sum 更新完整。

5. **冒烟启动**（可选，需 DB）：
   ```powershell
   go run ./cmd/server
   ```
   预期：服务启动，日志显示 valuation 配置加载完成，`/api/valuation/health` 可访问（需 JWT）。
